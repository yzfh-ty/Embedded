---
{"publish":true,"permalink":"/02_C语言/01_编译与调试.md","created":"2025-07-31T16:11:55.939+08:00","modified":"2025-08-05T19:59:24.547+08:00","cssclasses":""}
---

# 《C 语言程序设计》

1972 年 诞生于 贝尔实验室（BELL），作者：丹尼斯·麦卡利斯泰尔·里奇

# GCC 编译器

GCC 是 GNU 的编译器集合(GNU Compiler Collection)

安装 gcc

```shell
sudo apt install gcc -ysudo apt install g++ -y
```

第一个C 语言程序

C11 标准规定的两种main 函数

方法1(通常用于Windows系统):

文件名： myfrist.c

```c
int main(void) {
	return 0;
}
```

方法2(通常用于UNIX/Linux系统):

```c
int main(int argc, char * argv[]) 
{    
	return 0;
}
```

> 可以从命令行读取参数

## 编译

编译是指将 C 语言代码转化为 CPU 能够执行的数据和指令的过程。

### 命令

命令格式

```shell
gcc [选项]  文件1.c 文件2.c ...
```

运行

```shell
./a.out
```

常用选项

|   选项    |       说明        |
| :-----: | :-------------: |
| -o <路径> | 指定输出路径，代替a.out  |
|   -g    | 加入调试信息（用于GDB调试） |
| -I <路径> |    指定头文件的路径     |
| -l<库名>  |     指定库文件名称     |
|   -E    |    仅需处理，不编译     |
|   -S    |     编译，不汇编      |
|   -c    |   编译和汇编，但不连接    |
|  -Wall  |     报告所有警告      |

### C 语言编译的步骤:

1. 预处理 *gcc -E xxx.c*

```shell
gcc -E hello.c
```

1. 编译 gcc -S

```shell
gcc -S hello.c # 生成 hello.S 
```

1. 编译并汇编 gcc -c

```shell
gcc -c hello.S   # 生成 hello.o
```

1. 链接 *gcc -o a.out xxx.o yyy.o*

```shell
gcc -o hello hello.o  # 生成 hello
```


### 第一个hello world! 程序

文件名: hello.c

```c
#include <stdio.h>
int main(int argc, char * argv[]) 
{
	printf("hello world!\n");
	return 0;
}
```

编译:

```shell
gcc -o hello hello.c
```

运行:

```shell
./hello
```

说明:

- *# include <文件名.h>* 预处理包头文件
- 第一个 *int* 是 main 函数的返回的数据类型。
- *main* 是一个函数名，main 是程序的主入口函数。
- *(int argc, char * argv[])* 是 main 函数的参数列表。
- *{}* 包含内部的语句是main 的函数体部分。
- *pirntf();* 打印数据到终端.
- *"hello world!\n"* 打印的文字（是字符串）。*\n* 表示换行。
- *return 0;* 是返回函数, 返回值是零.

## 调试 （gdb）

gdb 是 GUN 的 Debuger 工具，可以用来调试 C或C++ 语言程序。

```shell
# 安装命令
apt install gdb
```

在编译时使用 `-g` 选项加入调试代码。

```shell
gcc -g xxx.c
```

运行调试

```shell
gdb 使用 gcc -g 编译后的程序  
如:  
gdb ./a.out  
....  
(gdb)
```

![[02_C语言/image/Pasted image 20250801140655.png]]

### 断点：

断点是程序调试的一个停止点。

断点设置命令

| 命令                |             | 示例           |
| ----------------- | ----------- | ------------ |
| break 函数名 或 b 函数名 | 在函数名设置断点    | b main       |
| b 文件名:行号          | 在指定的行号是设置断点 | b hello.c:20 |
| info b            | 查看所有的断点     |              |
| delete \<num\>    | 删除断点        |              |

### 调试命令

| 命令              | 说明              |
| --------------- | --------------- |
| `run` 或 `r`     | 从头运行程序遇到断点停止。   |
| `r arg1 arg2`   | 带启动参数运行         |
| `next` 或 `n`    | 单步执行一条语句        |
| `step` 或 `s`    | 单步进入当前函数内部      |
| `finish`        | 执行完当前函数并暂停      |
| `continue` 或`c` | 继续运行到下一个断点      |
| `list` 或 `l`    | 查看附近的代码         |
| p 表达式           | 打印表达式的值，如：`p x` |
| `quit` 或 `q`    | 退出调试            |

执行 `next` 之后显示的代码行是未执行的，在显示的代码行之前暂停

![[02_C语言/image/Pasted image 20250801141108.png]]


## 模块

C 语言中，一个C程序可以拆分成多个.c 文件。每一个.c 中都可以包含不同的 函数。我们把每个 .c 文件称之为模块

多模块的编译

```
prime.c -------> prime.o -\
                            + --  myprog
main.c  -------> main.o  -/

编译:将两个.c 文件生成两个.o 文件
gcc -c -o prime.o prime.c
gcc -c -o main.o main.c
将 两个.o 文件链接成 myprog 程序
gcc -o myprog main.o prime.o
```

生成 .o 文件

```shell
gcc -c -o xxx.o xxx.c
```

注意：

编译后的文件在链接时，需要保证函数和全局变量都是唯一的，不然会引起变量冲突。

##  预处理指令

C 预处理器(CPP)是编译过程中的独立阶段，在实际编译前对源代码进行文本处理。主要功能包括：
- 宏展开
- 文件包含
- 条件编译
- 特殊指令处理

**预处理器**不是编译器的组成部分，但是它是编译过程中一个单独的步骤。

简言之，C 预处理器只不过是一个文本替换工具而已，它们会指示编译器在实际编译之前完成所需的预处理。

两种用法

```c
#include <xxx.h>      // 优先查找库文件
#include "xxx.h"      // 优先查找本地文件,通常自己定义的函数使用双引号
```

| 指令         | 描述                            | 使用示例                                                                                                     |
| ---------- | ----------------------------- | -------------------------------------------------------------------------------------------------------- |
| `#define`  | 定义宏（符号常量或函数式宏）                | `#define PI 3.14159`  <br>`#define MAX(a,b) ((a) > (b) ? (a) : (b))`                                     |
| `#include` | 包含头文件                         | `#include <stdio.h>`  <br>`#include "myheader.h"`                                                        |
| `#undef`   | 取消已定义的宏                       | `#undef PI`                                                                                              |
| `#ifdef`   | 如果宏已定义则编译后续代码                 | `#ifdef DEBUG`  <br>`printf("Debug info\n");`  <br>`#endif`                                              |
| `#ifndef`  | 如果宏未定义则编译后续代码（常用于头文件保护）       | `#ifndef HEADER_H`  <br>`#define HEADER_H`  <br>`/* 内容 */`  <br>`#endif`                                 |
| `#if`      | 条件编译（可配合defined操作符使用）         | `#if VERSION > 2`  <br>`/* 新版代码 */`  <br>`#endif`                                                        |
| `#else`    | `#if`/`#ifdef`/`#ifndef`的替代分支 | `#ifdef WIN32`  <br>`/* Windows代码 */`  <br>`#else`  <br>`/* 其他系统 */`  <br>`#endif`                       |
| `#elif`    | 类似于else if                    | `#if defined(UNIX)`  <br>`/* Unix代码 */`  <br>`#elif defined(WIN32)`  <br>`/* Windows代码 */`  <br>`#endif` |
| `#endif`   | 结束条件编译块                       | 如上例所示                                                                                                    |
| `#error`   | 产生编译错误并输出消息                   | `#if !defined(C99)`  <br>`#error "需要C99标准"`  <br>`#endif`                                                |
| `#pragma`  | 编译器特定指令（非标准，各编译器不同）           | `#pragma once`  <br>`#pragma pack(1)`                                                                    |

示例

```c
#include <stdio.h>

// 定义常量宏
#define PI 3.1415926
#define GREETING "Hello, World!"

// 定义函数式宏（注意括号的使用）
#define SQUARE(x) ((x) * (x))
#define MAX(a, b) ((a) > (b) ? (a) : (b))

// 条件编译示例
#define DEBUG 1

int main() {
    // 使用常量宏
    printf("PI的值: %f\n", PI);
    printf("%s\n", GREETING);
    
    // 使用函数式宏
    int x = 5;
    printf("%d的平方是: %d\n", x, SQUARE(x));
    printf("3和5中较大的数是: %d\n", MAX(3, 5));
    
    // 条件编译示例
    #ifdef DEBUG
    printf("[调试信息] 程序运行到main函数\n");
    #endif
    
    // 编译器版本检查
    #if __STDC_VERSION__ >= 201112L
    printf("使用C11标准\n");
    #elif __STDC_VERSION__ >= 199901L
    printf("使用C99标准\n");
    #else
    printf("使用C89/C90标准\n");
    #endif
    
    // 错误指令示例（取消注释将导致编译错误）
    // #error "这是一个手动触发的错误"
    
    return 0;
}
```

## 计算机中常用的段

代码段

数据段

堆

栈