---
{"publish":true,"permalink":"/02_C语言/14_字符串.md","created":"2025-08-01T08:51:17.357+08:00","modified":"2025-08-01T08:58:55.028+08:00","cssclasses":""}
---

# 字符串

什么是字符串

字符串是用来保存文字数据的一种数据类型。可以认为字符是 char 类型的一维数组（最后一个字符一定是零`'\0'`）。

字符串的字面值

用双引号括起来。内部是文字信息。

```c
"hello world!" // 函数13个字符的字符串。
"abc"  // 长度是4
```

## 字符的转义

当字符串字面值的内部有 控制字符或 双引号是，可以使用一个反斜杠加特殊字符表示一个字符，叫做字符的转义，如：

```c
\" 表示双引号  
\' 表示单引号  
\n 表示换行  
\t 表示制表符  
\xHH. hh 表示2位16进制的编码，如\x41 表示 'A'
```

## 字符串的保存

使用char 类型的数组来保存字符串。

```c
char text[100];
char text[100] = {'A', 'B'}
char text[100] = "ABC"; //数组长度为 100,其余为0（'\0'）
char text[] = "ABC";  // 自动推断数组长度为 4。
```

printf 输出字符使用 `%s`占位符

scanf 读取字符串也使用 `%s` 占位符
