---
{"publish":true,"permalink":"/02_C语言/21_清除输入缓存区.md","created":"2025-08-04T19:57:42.962+08:00","modified":"2025-08-05T17:28:25.831+08:00","cssclasses":""}
---

以下面这个题目为例

循环输入几个学生的姓名，年龄和身高，当提示输入姓名时，直接回车退出，打印你输入的所有学生的姓名，年龄和身高。

```c
#include <stdio.h>

struct student
{

    char name[50];  // 姓名
    int age;        // 年龄
    float height;   // 身高

};

int main(int argc, char *argv[])
{
    // 定义100个学生类型的数组 class1;
    struct student class1[100];

    int stu_count = 0;  // 用于记录学生数;

    for (int i = 0; i < sizeof(class1) / sizeof(class1[0]); i++)
    {
        printf("请输入姓名: ");
        scanf("%s", class1[i].name);
        // if (class1[i].name == '\n')
	    //     break;
	    if (class1[i].name == '\n')  // 这两种方法都无效
		    break;
        printf("请输入年龄: ");
        scanf("%d", &class1[i].age);
        printf("请输入身高: ");
        scanf("%f", &class1[i].height);
        stu_count++;
    }

    for (int i = 0; i < stu_count; i++)
    {
        printf("name:%s, age:%d, height:%f\n",
               class1[i].name,
               class1[i].age,
               class1[i].height);
    }
    return 0;

}
```

