---
{"publish":true,"permalink":"/linux/03 系统包管理命令.md","created":"2025-07-30T20:37:26.521+08:00","modified":"2025-07-31T08:12:52.928+08:00","cssclasses":""}
---

# 系统包管理工具

### apt 命令 - [[linux/04 更换国内镜像源]]

作用：安装、下载、删除第三方软件。他是用于管理软件包的工具

命令格式

```shell
apt 子命令 包名
```

子命令:
``
```shell
apt update               #更新软件包索引
apt upgrade              #升级所有已经安装的软件包
apt install <包名>       #安装软件包
apt remove <包名>        #卸载软件包（不删除依赖包）
apt purge <包名>         #卸载软件包（删除依赖包）
apt list                #列出所有可以安装的软件包
apt list installed      #列出所有已经安装的软件包
apt search <关键字>      #搜索软件包
apt download <包名>      #下载软件包，不安装。
```

示例

```shell
# 看有哪些tree相关的软件包
apt search tree | grep "tree"
# 安装 tree 软件包
sudo apt install tree
# 使用 tree 命令查看 文件夹的结构
tree /etc/apttree ~tree .
# 卸载 tree 软件包
sudo apt remove tree
# 下载 tree 软件包，后续在安装
apt download tree
```

### dpkg 命令

作用：用于安装和卸载离线包(.deb包）

命令格式

```shell
sudo dpkg [选项] 包名
```

常用选项

| 选项       | 说明    |
| -------- | ----- |
| -i <包文件> | 安装    |
| -r <包文件> | 卸载软件包 |
